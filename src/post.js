
const item = (props) => {
    return(
        <div>
            
            <div className="funcs">
                <div>{props.strp}</div>
            </div>
        
        </div>

    );
}

export default item ;